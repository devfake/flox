#!/bin/bash

WORKER=${1:-1}

while [[ $WORKER > 0 ]]; do
  let WORKER-=1
  nohup php backend/artisan queue:work --tries=3 --daemon >/dev/null 2>/dev/null &
  echo $! >> ./bin/.worker.pid
  echo "Start worker #$!"
done
